{
  "name": "react-native-audio-chunking",
  "version": "1.0.43",
  "description": "React Native module for chunked audio recording",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["react-native", "audio", "recording", "chunking"],
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "react-native": ">=0.60.0"
  },
  "react-native": {
    "ios": {
      "podspec": "AudioChunkingModule.podspec"
    }
  }
}
